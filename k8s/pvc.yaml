apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: opensearch-alert-data
  namespace: k8s-app
  labels:
    app: opensearch-alert
    version: "v3.5.0"
spec:
  storageClassName: nfs-provisioner-storage
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
